export type Credentials = {
    SecretKey: string;
    PublicToken: string;
    DeviceToken: string;
    BearerToken: string;
};
export type Action = `whatsapp` | 'vehicles' | 'correios' | 'dados/cpf' | 'dados/cnpj' | 'cep';
export declare const createApiInstance: (action: Action, credentials: Credentials) => {
    request: (endpoint: string, data: Record<string, any>) => Promise<any>;
};
